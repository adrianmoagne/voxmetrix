{
	"name": "voxmetric",
	"version": "0.1.0",
	"private": true,
	"description": "Open-source monorepo for the VoxMetric experiment platform.",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "npm-run-all --parallel dev:*",
		"dev:web": "npm run dev -w @voxmetric/web",
		"dev:server": "npm run dev -w @voxmetric/server",
		"build": "npm run build -w @voxmetric/server && npm run build -w @voxmetric/web",
		"lint": "npm run lint -w @voxmetric/web",
		"test": "npm run test -w @voxmetric/server"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
